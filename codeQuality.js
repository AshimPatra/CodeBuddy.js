async function getUsers(users) {
    var new_users = [];
 //A Standard for-loop with length caching   
  var i=0,len=users.length;
    while(i<len){
        var newUser = new_users.push(user[i].id=i)
        i++;
    }
    return newUser;
}

